# Chat Logging

Use this skill to guarantee that prompts and delegated tasks are written to `CHATS`.

## Steps

1. Use hooks when available.
2. If the entry is missing, append it manually using the format from `AGENTS.md`.
3. Include the delegated agent when subagent work is involved.